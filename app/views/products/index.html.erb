<h1>Products list</h1>

<%# Code for the Map %>
<% if user_signed_in? %>
  <div class="accordion pb-4" id="accordionExample">
    <div class="accordion-item">
      <h2 class="accordion-header" id="headingOne">
        <button class="accordion-button fs-6 lh-1" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
          Map
        </button>
      </h2>
      <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
        <div class="accordion-body">
          <div id="map" style="width: 100%; height: 500px;"
            data-controller="map"
            data-map-markers-value="<%= @markers.to_json %>"
            data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>

<%# Code for the cards %>
<div class="container">
  <div>
    <h3> <%=link_to "Add new product", new_product_path %> </h3>
  </div>
  <div class="card d-flex">
    <% @products.each do |product|%>
      <%# <div class="card-title" link_to product.title, product_path(product)> %>
      <div class="card-body">
        <h5 class="card-title"> <strong>Title:</strong> <%= product.title%> </h5>
        <div class="col-4">
          <%= render 'photo_card', product: product %>
        </div>
        <p class="card-sub"> <strong>Price:</strong> <%= product.price %> </p>
        <p class="card-text"> <strong>Description:</strong> <%= product.description %> </p>
      </div>
    <% end %>
  </div>
</div>
<%#
<div class="container">
  <h2 class="text-center mt-3">Recent Products</h2>
  <div class= "card  d-flex  flex-row align-items-stretch">
    <% @products.each do |product|%>
      <%# <div class=" card-body  h-100 p-3">
        <a href=<%= product_path(product) %>
          <%# <div class="img-link mb-3"><%= render 'instrument', product: product %></div>
          <%# <div class="card-text ">
            <h2 ><%= product.title %></h2>
            <%# <p><%= truncate(product.description, length: 150) %></p>
          <%# </div>
        </a>
      </div>
      <br>
    <% end %>
  <%# </div>
</div>
